services:
  dinner-party-beta:
    build: .
    ports:
      - "5000:5000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - BETA_ACCESS_CODE=${BETA_ACCESS_CODE:-THAMES_CLUB_VIP}
      - PORT=5000
